#!/bin/bash

function get_status(){
  if [[ "$(cat /var/vcap/sys/run/keepalived/keepalive.*.state)" == "MASTER" && \
        -f /var/vcap/sys/run/keepalived/keepalived.pid ]]; then
    echo "up"
   else
    echo "down"
  fi
}

get_status
