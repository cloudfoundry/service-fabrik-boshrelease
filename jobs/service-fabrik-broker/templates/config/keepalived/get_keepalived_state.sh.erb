#!/bin/bash

function get_status(){
  if [[ "$(cat /var/run/keepalive.*.state)" == "MASTER" && \
   "$(sudo service keepalived status | awk '{print $2}')" = *"running"* ]]; then
    echo "up"
   else
    echo "down"
  fi
}

get_status
